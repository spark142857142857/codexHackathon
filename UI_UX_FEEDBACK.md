# Market Signal Atlas UI/UX 업데이트 피드백

## 목표

현재 화면의 신뢰성·차분한 리서치 톤은 유지하되, **AI가 자동 생성한 랜딩 페이지처럼 보이는 인상**을 줄이고 “시장 이벤트를 빠르게 읽는 전문 도구”로 바꾼다. 우선순위는 장식보다 정보 위계와 사용자의 첫 행동이다.

## 현재 화면에서 느껴지는 문제

1. **첫 화면의 행동 유도가 핵심 기능과 어긋난다.**
   히어로의 주요 CTA인 `전체 데이터 보기`는 바로 아래의 핵심 사례 탐색이 아니라 대규모 Signal Universe로 이동한다. 처음 온 사용자는 서비스의 대표 기능보다 데이터 파이프라인부터 보게 된다.

2. **탭과 필터가 기능 맥락에서 분산된다.**
   좌측 사이드바는 섹션 이동, 상단은 반응형 섹션 이동, 사례 탐색에는 출처 탭, 차트에는 렌즈 탭, 하단에는 Universe scope 탭이 있다. 각각은 합리적이지만, 한 화면에서 탭의 목적이 겹쳐 보인다.

3. **차트가 핵심 증거인데 면적과 위계가 부족하다.**
   3열 구조에서 중앙 패널이 원문·KPI·탭·차트·4단계 해석을 모두 담아 차트 높이가 제한된다. 사용자는 숫자와 원문을 많이 읽지만 시장 반응의 형태는 작게 본다.

4. **타이포그래피가 지나치게 작고 균질적이다.**
   여러 보조 텍스트가 7~10px이라 데스크톱에서도 읽기 어렵고, 작은 대문자·배지·아이콘이 많아 “AI 대시보드 템플릿” 같은 밀도를 만든다. Georgia 인용문과 산세리프 UI가 섞이지만 역할 구분도 충분히 강하지 않다.

5. **‘AI Research Desk’가 실제 가치보다 앞선다.**
   6단계 파이프라인은 투명성에는 좋지만, 모든 사건에서 상시 노출되면 “AI가 분석했다”는 인상을 강화한다. 현재 실제 구현은 결정론적 규칙 기반이므로 용어와 배치가 이를 정확히 반영해야 한다.

## 권장 정보 구조

### 데스크톱 첫 화면

```text
[로고]  [반응 비교] [시그널 탐색] [인사이트] [분석 방법]     [한국어]

서비스 설명 + 현재 데이터 범위/마지막 갱신
┌────────────────── 핵심 기능 3개 ──────────────────┐
│ ① 시장 변동에서 시그널 찾기  ② 사건별 반응 비교  ③ 근거 검증 │
└─────────────────────────────────────────────────┘

자산/기간/인물/주제 필터
┌──────────────────── 넓은 시장 타임라인 + 사건 마커 ────────────────────┐
└───────────────────────────────────────────────────────────────────────┘

[대형 가격/peer 비교 차트]
[해당 구간의 사건 목록]     [선택 사건의 원문·핵심지표]       [연결 근거·신뢰 상태]
```

이 순서는 서비스가 무엇을 하는지 → 무엇을 먼저 조작할지 → 왜 믿을 수 있는지를 자연스럽게 만든다. 전체 원문 검색과 6단계 리포트는 핵심 화면 아래의 심화 도구로 이동한다.

### 전역 메뉴 우선순위: `반응 비교`가 먼저여야 한다

현재 사이드바는 `시그널 탐색 → 반응 비교 → 데이터 출처 → 분석 방법` 순서다. 하지만 제품의 주된 사용 가치는 원문을 많이 찾는 데 있지 않고, **시장 반응의 크기·형태·비교군을 먼저 파악한 뒤 그 원인이 될 수 있는 공개 시그널을 검토하는 데** 있다. 따라서 전역 메뉴와 최초 진입 섹션은 아래 순서를 권장한다.

1. **반응 비교** — 자산/기간을 고르고 가격·초과수익률·peer의 움직임을 본다.
2. **시그널 탐색** — 선택한 반응 구간과 연결된 원문·주제·출처를 좁혀 본다.
3. **인사이트** — 토픽/인물/자산 단위의 패턴을 비교한다.
4. **데이터 출처·분석 방법** — 데이터 범위, 신뢰 상태, 계산식을 검토한다.

`Signal Explorer`는 제거 대상이 아니라 **반응 비교의 상세 드릴다운**으로 재배치하는 것이 핵심이다. 예를 들어 반응 차트에서 변동 구간을 클릭하면 해당 기간의 시그널 목록이 즉시 필터된 상태로 열리게 한다. 이렇게 하면 사용자는 “무슨 말을 했나?”보다 먼저 “무엇이 실제로 움직였나?”에 답할 수 있다.

## 구체적인 UI 개선안

### 1. 히어로를 짧고 기능 중심으로 재구성 — P0

- 히어로 높이를 현재의 큰 소개 영역보다 줄이고, 한 문장 가치 제안과 데이터 범위만 남긴다.
- 주요 CTA는 `시장 타임라인 탐색`으로 바꾸어 첫 핵심 화면으로 이동시킨다.
- 보조 CTA는 `분석 방법 보기` 또는 `전체 원문 검색`으로 둔다.
- 레이더 궤도 장식은 축소하거나 제거한다. 대신 최신 데이터 상태, 지원 자산, 직접/정책/프록시 표기 범례를 작게 제시한다.
- 히어로 바로 아래에 카드 3개로 주요 기능을 제시한다.
  - **Market Timeline**: 가격 변동 시점의 공개 발언 찾기
  - **Event Comparison**: 사건별 자산·benchmark·peer 비교
  - **Evidence Trail**: 원문, 뉴스, 데이터 범위와 한계 확인

### 2. 내비게이션과 탭의 위치를 단순화 — P0

- 데스크톱에서는 좌측 고정 사이드바 또는 상단 내비게이션 중 하나만 주 내비게이션으로 사용한다. 권장안은 **상단 고정 내비게이션**이다. 가로 폭을 차트에 더 쓸 수 있고, 제품형 서비스에 더 자연스럽다.
- 주 메뉴의 첫 항목과 기본 앵커는 `반응 비교`로 둔다. URL 진입, 로고 클릭, 히어로 CTA도 모두 이 섹션으로 연결한다. `시그널 탐색`은 두 번째 메뉴로 두고 반응 구간을 클릭했을 때의 상세 탐색 화면으로 연결한다.
- 좌측 영역은 “사건 목록 + 저장한 필터”에 사용한다. 지금처럼 페이지 전체 섹션 이동에 쓰지 않는다.
- `출처 탭`은 사건 목록 위의 필터 그룹으로 통합한다. 예: `모두 | 소셜 | 뉴스룸 | 공시 | 청문회`.
- 차트 렌즈 탭은 차트 제목 바로 아래가 아니라 **차트 오른쪽 상단**에 작게 둔다. 기본 선택은 `시장 반응`으로 하고, `전체 비교 / 뉴스 / 관심도`는 보조 보기로 둔다.
- 모바일에서는 상단에 핵심 3개만 남긴다: `타임라인`, `사건`, `인사이트`. 방법론은 더보기 메뉴로 이동한다.

### 3. 차트를 확장하고 읽는 법을 강화 — P0

- 선택 사건의 주요 차트 높이를 최소 `360~440px`으로 올린다. 현재 원문 카드 내부가 아니라 독립된 넓은 섹션으로 분리하는 것이 좋다.
- 한 번에 두 개의 시각화를 제공한다.
  - 상단: 자산/benchmark/peer의 누적 초과수익률 라인 차트
  - 하단 또는 우측: 뉴스량·추적 관심도를 작은 보조 막대 차트
- 사건일에는 세로 기준선, 사건 마커, `발언 시각 / 장 마감 후 정렬` 라벨을 명확히 표시한다.
- Tooltip에는 실제 종가, 일간 수익률, 초과수익률, 거래량 배수, 출처 정밀도를 같은 순서로 표시한다.
- 색은 자산마다 고정한다. 예: linked asset=짙은 네이비, benchmark=회색, peers=옅은 청록/보라. 초록색을 상승 전용으로 쓰지 말고 상승/하락은 수치와 방향 아이콘으로 병행한다.
- 데이터가 없는 뉴스/관심도 렌즈는 빈 그래프보다 `해당 기간 근거 없음` 상태 패널을 표시한다.

### 4. 타이포그래피와 밀도 개선 — P1

- UI 기본 글꼴은 한글·영문 모두 안정적인 산세리프 하나로 통일한다. 예: `Pretendard Variable, Inter, system-ui, sans-serif`. 외부 폰트를 추가할 수 없다면 시스템 폰트 우선으로 둔다.
- 원문 인용만 serif를 사용한다. Georgia는 영어 원문에만 제한하고, 한국어 요약에는 산세리프를 사용한다.
- 본문 최소 크기를 `14px`, 보조 본문을 `12px`, 메타데이터를 `11px` 이상으로 올린다. 7~9px 텍스트는 차트 축·짧은 배지 외에는 제거한다.
- 제목 단계는 명확히 3개로 제한한다: 페이지 32~40px, 섹션 24~28px, 카드 16~18px.
- 대문자 kicker, 아이콘+배지, 테두리를 모두 줄인다. 카드마다 아이콘을 넣기보다 중요한 상태에만 아이콘을 사용한다.
- 라일락 배경·보라 포인트는 유지하되, 페이지 전체 그라데이션과 장식 그리드는 약화한다. 흰 배경과 얇은 구분선으로 정보 밀도를 높인다.

### 5. “AI” 표현을 정확한 역할로 변경 — P1

- `AI Research Desk`는 `Evidence Review` 또는 `근거 검토`로 이름을 변경한다.
- 실행 버튼은 `근거 요약 생성` 또는 `검토 리포트 보기`로 바꾸고, 바로 옆에 `규칙 기반 · 예측 아님`을 표시한다.
- 6단계는 기본적으로 접힌 `검토 과정 보기`로 제공하고, 첫 화면에는 판정·신뢰도·주요 한계만 보인다.
- “AI-ready” 배지는 제품 전체에서 한 번만 노출한다. 반복 노출은 기능 가치보다 기술 유행어가 앞서 보이게 한다.

### 6. 첫 화면 KPI를 사용자 질문으로 교체 — P1

현재 KPI는 사례 수·출처 유형·가장 큰 반응·RSS 연결 상태다. 데이터 운영 상태는 유용하지만, 사용자 질문을 더 직접 반영한다.

| 권장 KPI | 보여줄 값 | 클릭 시 이동 |
| --- | --- | --- |
| 오늘/최근 시그널 | 시장 세션 정렬 대기·완료 건수 | 최신 사건 목록 |
| 가장 큰 반응 | 자산·사건·절대 1일 초과수익률 | 해당 사건 상세 |
| 주목할 토픽 | 표본 수가 충분한 토픽의 중앙값 반응 | 토픽 인사이트 |
| 데이터 상태 | 마지막 가격·신호 갱신 시각, stale 여부 | 출처/상태 상세 |

### 7. Signal Universe의 위치와 표현 변경 — P2

- 32,393개 원문·군집 파이프라인은 차별점이지만, 메인 탐색보다 먼저 나오면 제품이 데이터셋 뷰어처럼 보인다.
- 첫 화면에서는 `전체 원문 32,393개 검색 가능`이라는 작고 신뢰를 주는 링크/배지로 축약한다.
- 전체 검색은 별도 페이지 또는 메인 하단의 심화 섹션으로 이동한다.
- 파이프라인 도식은 출시/발표용 “방법론” 영역에 남기고, 일상 사용 흐름에서는 노출을 줄인다.

## 구현 순서 제안

1. CTA 목적지 변경, 히어로 축소, 핵심 기능 3개 노출
2. 좌측 전역 내비게이션 제거 또는 상단으로 통합; 출처·렌즈 탭 위치 정리
3. 선택 사건 차트를 독립 대형 영역으로 분리하고 peer 비교 추가
4. 기본/보조 글자 크기와 카드·배지 밀도 조정
5. AI 용어를 Evidence Review로 정리하고 6단계 패널 접기
6. Universe를 메인 탐색 뒤로 이동

## 완료 기준

- 첫 방문자가 5초 안에 “공개 발언과 시장 반응을 연결해서 보는 도구”라고 설명할 수 있다.
- 첫 클릭이 전체 데이터셋이 아니라 시장 타임라인 또는 대표 사건으로 이어진다.
- 1440px 폭에서 선택 사건의 주 차트가 스크롤 없이 충분히 읽히고, 텍스트가 11px 미만에 의존하지 않는다.
- 탭의 역할을 사용자가 구별할 수 있다: 전역 이동, 사건 필터, 차트 보기 중 하나만 각 위치에 존재한다.
- AI는 결론을 대신 내리는 기능이 아니라, 재현 가능한 근거 검토를 돕는 기능으로 인식된다.
