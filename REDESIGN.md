# Market Signal Atlas — REDESIGN 적용 가이드

이 문서는 현재 반영된 Market Signal Atlas의 화면 디자인과 UX 구조를 다른 작업자의 로컬 환경에서 동일하게 구현하기 위한 기준이다. 단순히 색상만 바꾸는 문서가 아니라, **정보를 읽는 순서·데이터의 신뢰 상태·기존 기능의 위치**까지 보존하는 구현 명세다.

## 1. 목표와 원칙

### 디자인 목표

- 금융 트레이딩 화면처럼 압박감 있는 UI가 아니라, 차분한 **리서치·검증 도구**로 보이게 한다.
- 사용자가 한 사례를 볼 때 `원문 → 시장·뉴스 반응 → 단계별 해석 → AI 검증 과정` 순서로 이해하게 한다.
- 데이터가 없거나 실시간 요청이 실패한 경우 수치를 만들지 않는다. 반드시 **검토 스냅샷** 또는 **값 없음**을 표시한다.
- 기존 기능(사례 탐색, 필터, 실제 시장 가격, GDELT 뉴스, 관심도, AI 리서치, 출처·방법론)은 제거하지 않는다.

### 화면 우선순위

1. 좌측: 지금 어느 연구 섹션에 있는지
2. 중앙: 선택된 시그널의 근거와 반응 데이터
3. 우측: AI 분석 파이프라인과 신뢰도
4. 중앙 하단: 사람이 이해할 수 있는 4단계 해석

## 2. 적용 대상 파일

| 역할 | 파일 |
| --- | --- |
| 전체 레이아웃·색상·반응형 | `app/globals.css` |
| 좌측 내비게이션, 사례 탐색, 차트, 해석 패널 | `components/signal-atlas-dashboard.tsx` |
| 실시간 시장 데이터 | `app/api/live/route.ts`, `lib/live.ts` |
| 사례별 GDELT 뉴스 | `app/api/news/route.ts`, `lib/gdelt.ts` |
| 공통 데이터 타입 | `lib/types.ts` |

## 3. 데스크톱 레이아웃

### 전체 구조

화면은 다음의 2단 구조를 기본으로 한다.

```text
┌──────────────── 좌측 260px ────────────────┬──────────────────── 콘텐츠 영역 ────────────────────┐
│ Signal Atlas                                │ 상단 헤더                                              │
│                                              ├──────────────────────────────────────────────────────┤
│ RESEARCH                                     │ 히어로 + 핵심 지표                                    │
│ [시그널 탐색]                                ├──────────────────────────────────────────────────────┤
│  반응 비교                                   │ 사례 목록 │ 원문·차트·4단계 해석 │ AI 리서치 데스크 │
│  데이터 출처                                 │           │                      │                  │
│  분석 방법                                   ├──────────────────────────────────────────────────────┤
│                                              │ 반응 비교 / 데이터 출처 / 분석 방법                    │
│ 검토 사례 수 · 원문 출처 유형 · 면책 문구   │                                                          │
└──────────────────────────────────────────────┴──────────────────────────────────────────────────────┘
```

### 좌측 내비게이션

- 너비: `260px`, `position: sticky`, `top: 0`, `height: 100vh`
- 배경: 흰색, 우측 경계선 `#e2e6ee`
- 메뉴 순서: `시그널 탐색`, `반응 비교`, `데이터 출처`, `분석 방법`
- 활성 메뉴는 연한 보라 `#eeeaff`, 글자 `#4e3bb0`, 굵기 `700`
- hover는 `#f5f2ff`와 `#483897`을 사용한다.
- 하단에는 검토 사례 수, 원문 유형 수, 연구·모니터링 도구 면책 문구를 둔다.

#### 활성 상태 구현 규칙

좌측 메뉴는 앵커 링크(`#explorer`, `#comparison`, `#sources`, `#methodology`)로 이동한다. 클릭한 메뉴의 활성 색이 스크롤 감지에 의해 이전 메뉴로 즉시 되돌아가면 안 된다.

`SignalAtlasDashboard`에서 다음 패턴을 유지한다.

```tsx
const [activeNav, setActiveNav] = useState("explorer");
const navClickLock = useRef<string | null>(null);

// 클릭: 이동 대상의 선택 상태를 즉시 보여준다.
onClick={() => {
  navClickLock.current = id;
  setActiveNav(id);
}}

// IntersectionObserver: 클릭한 섹션이 실제 관찰 영역에 들어오면 lock을 해제한다.
if (navClickLock.current) {
  const clicked = entries.find(
    (entry) => entry.target.id === navClickLock.current && entry.isIntersecting,
  );
  if (clicked) {
    setActiveNav(clicked.target.id);
    navClickLock.current = null;
  }
  return;
}
```

이 동작은 hover가 아니라 실제 클릭 상태를 표현하기 위한 필수 기능이다.

## 4. 색상과 시각 톤

기본 톤은 밝은 라일락·네이비·차분한 그린이다. 보라색은 탐색·상태·AI 분석을, 그린은 실제 시장 데이터와 긍정적 반응을 나타낸다. 강한 네온, 과도한 그라데이션, 트레이딩 앱의 매수/매도 UI는 사용하지 않는다.

| 용도 | 색상 |
| --- | --- |
| 페이지 배경 | `#f7f8ff` 계열 |
| 본문 카드 | `#ffffff` |
| 텍스트 네이비 | `#242747` / `#30345c` |
| 보조 텍스트 | `#6d7691` / `#78809d` |
| 활성 보라 | `#5f59de` / `#4e3bb0` |
| 활성 배경 | `#eeeaff` |
| 실제 가격 그린 | `#1d7651` |
| 시장·뉴스 성공 상태 | 연한 그린 `#f1faf5`, 테두리 `#c9e2d4` |
| 구분선 | `#dce1f1` |
| 출처 섹션 배경 | `#28294c` |

카드는 8~12px의 부드러운 radius와 약한 그림자만 사용한다. 카드 내부의 수치와 문장에는 충분한 여백을 주어 사용자가 한 번에 하나의 질문에 집중할 수 있게 한다.

## 5. 사례 탐색 영역

### 3열 연구 워크스페이스

데스크톱에서 `.explorer-grid`는 아래 비율을 사용한다.

```css
grid-template-columns: minmax(270px, .78fr) minmax(440px, 1.45fr) minmax(260px, .72fr);
min-height: 705px;
```

| 열 | 내용 | UX 목적 |
| --- | --- | --- |
| 좌측 | 사례 목록 | 여러 시그널을 빠르게 비교·선택 |
| 중앙 | 원문, KPI, 반응 차트, 해석 | 한 사례를 근거 중심으로 읽기 |
| 우측 | AI 리서치 데스크 | 결론이 생성된 검증 단계를 확인 |

사례 목록에서 선택된 행은 연한 보라 배경과 좌측 보라 라인으로 표시한다. 원문 문장은 Georgia 계열 serif를 사용해 보고서의 읽기감을 만든다.

### 중앙 근거 패널의 순서

중앙 패널은 반드시 다음 순서를 유지한다.

1. 인물/기관, 출처 유형, 주제 배지
2. 한국어 요약 인용문과 영어 원문 발췌
3. 정확한 시각 또는 날짜 정밀도, 원문 링크
4. `1일 초과수익률 · 거래량 배수 · 3일 지속성` KPI 3개
5. 반응 렌즈와 차트
6. 데이터 출처 상태
7. 4단계 해석
8. 관심도 근거와 범위

## 6. 반응 차트와 데이터 신뢰 상태

### 반응 렌즈

`전체 비교`, `시장`, `뉴스`, `대중 관심`의 4개 버튼을 제공한다.

- **전체 비교**: 실제 종가 + 뉴스 발행량 + 추적 관심도
- **시장**: 선택 자산의 실제 종가만 표시
- **뉴스**: GDELT 일별 뉴스 발행량과 대표 기사
- **대중 관심**: 추적 계정 언급과 해시태그 빈도

Recharts 차트의 `Line`, `Bar`에는 `isAnimationActive={false}`를 준다. 첫 렌더링 때 애니메이션이 완료되기 전 그래프가 비어 보이는 현상을 방지하기 위한 필수 설정이다.

### GDELT 상태 표현

뉴스 렌즈 하단에는 `.news-source-status`를 둔다.

- 실시간 성공: `실시간 GDELT 조회 · N일 뉴스량 · 대표 기사 N건`
- 로딩: `GDELT 뉴스 데이터를 조회하는 중입니다…`
- 실패/요청 제한: `검토용 뉴스 스냅샷`임을 명시
- 과거 스냅샷 자체도 없으면: `값을 추정하지 않고 비워 둡니다.`

실시간 결과가 있으면 대표 기사 최대 2건을 도메인, 제목, 외부 링크 아이콘과 함께 `.news-headlines`에 표시한다. 기사 수·뉴스량·출처 상태는 서로 다른 근거이므로 하나의 점수로 합치지 않는다.

## 7. 4단계 해석 패널

시장 지표와 차트를 먼저 계속 노출한 뒤, 바로 아래에 `.interpretation-panel`을 배치한다. 이 패널은 차트를 대체하지 않는다.

| 단계 | 제목 | 표시할 내용 |
| --- | --- | --- |
| 01 | 무슨 일이 있었나 | 원문, 출처, 시간 정밀도, 원문 링크 |
| 02 | 왜 연결됐나 | 자산 연결 근거와 벤치마크 선택 이유 |
| 03 | 어떻게 반응했나 | 초과수익률·거래량·지속성의 읽는 법 |
| 04 | 무엇을 유의하나 | 시간적 연관성과 인과의 차이, 누락 가능성 |

- 4개의 탭은 가로로 동등한 폭을 가진다.
- 활성 단계는 흰 배경, 보라/그린 계열 텍스트로 표시한다.
- 콘텐츠 최소 높이는 약 `128px`로 고정해 단계 전환 시 화면이 크게 흔들리지 않게 한다.
- `무엇을 유의하나` 단계에는 “시간적 연관성을 원인으로 단정하지 않습니다.”라는 안전 문구를 항상 넣는다.

## 8. AI 리서치 데스크

우측 패널에는 다음을 유지한다.

- 증거 판정: 반응 관찰 / 혼합된 증거 / 증거 부족
- 신뢰도
- AI 리서치 실행 버튼
- 6단계 파이프라인: 분류 → 온톨로지 매핑 → 정보 확산 → 시장 반응 → 신뢰도 감사 → 리포트 작성
- 각 단계별 상태와 요약

이 영역은 “AI가 결론을 냈다”는 인상을 주는 곳이 아니라, 사용자가 결론의 생성 과정을 검토하는 곳이다. 그러므로 확정적 투자 권유 문구를 넣지 않는다.

## 9. 반응형 기준

### 1050px 이하

- 좌측 사이드바를 숨긴다.
- 상단 헤더 안에 동일한 섹션 링크(`.top-section-nav`)를 노출한다.
- 기능 중복을 막기 위해 데스크톱에서는 상단 섹션 링크를 숨긴다.
- 탐색 영역은 사례 목록 + 근거/AI 영역의 2열로 축소한다.

### 760px 이하

- `.explorer-grid`를 1열로 전환한다.
- 사례 목록의 고정 높이/스크롤은 해제해 카드처럼 이어지게 한다.
- KPI, 차트 렌즈, 4단계 해석 탭은 2열 또는 읽기 가능한 크기로 줄인다.
- 데이터 표는 가로 스크롤 또는 카드형 표현을 사용하되 출처·상태 정보는 생략하지 않는다.

## 10. 기능 보존 체크리스트

디자인 적용 뒤 아래 항목이 모두 동작해야 한다.

- [ ] 좌측 메뉴 클릭 시 해당 앵커로 이동하고 활성 색이 선택한 메뉴에 유지된다.
- [ ] 스크롤로 섹션을 이동해도 현재 섹션에 맞게 메뉴 활성 상태가 바뀐다.
- [ ] 사례 목록 클릭 시 원문, KPI, 차트, 해석, AI 패널이 함께 갱신된다.
- [ ] `전체 비교 / 시장 / 뉴스 / 대중 관심` 탭이 실제로 차트 구성을 바꾼다.
- [ ] 시장 차트는 애니메이션 대기 없이 첫 표시부터 보인다.
- [ ] Twelve Data가 연결되면 출처 표에 Fresh 상태와 최신 가격 시각이 보인다.
- [ ] GDELT 성공/실패/스냅샷 상태가 서로 구분돼 보인다.
- [ ] 4단계 해석은 각각 다른 내용을 표시한다.
- [ ] AI 리서치 실행 시 단계 정보가 갱신된다.
- [ ] 한국어(`/ko`)와 영어(`/`) 모두 같은 정보 구조를 유지한다.

## 11. 로컬 검증 명령

```bash
npm run lint
npm run build
npm run dev
```

브라우저에서 `/ko`를 열고, 좌측의 `반응 비교`, `데이터 출처`, `분석 방법`을 차례로 클릭한다. 각 클릭 뒤 해당 메뉴만 활성 보라색 배경이 남아 있는지 확인한다.

